# Original Contributor: Lex Black <autumn-wind@web.de>
# Source: https://aur.archlinux.org/cgit/aur.git/tree/PKGBUILD?h=opencloud-desktop-git

_pkgname=opencloud-desktop
pkgname=opencloud-desktop-bin
# Version/rel is updated by CI, do not edit manually
pkgver=3.0.3
pkgrel=5
pkgdesc='opencloud desktop application (binary package)'
arch=('x86_64')
url="https://github.com/hypergonial/opencloud-desktop-bin"
license=('GPL-2.0-only')
depends=(gcc-libs
         glibc
         kdsingleapplication
         libre-graph-api
         qt6-base
         qtkeychain-qt6
         sqlite
         zlib)
makedepends=(doxygen
             extra-cmake-modules
             git
             python-sphinx
	         qt6-declarative
             qt6-tools)
conflicts=('opencloud-desktop')
provides=('opencloud-desktop')
source=("${_pkgname}::git+https://github.com/opencloud-eu/desktop.git#tag=v${pkgver}")
# Updated by CI if pkgver changes
# Run 'updpkgsums' to update this value if changing the 'source' array
sha256sums=('88cd6ea12aa9f84ccb31d66b573ae81837db3964d8f826ae2400a4ddabf776fb')

build() {
	cmake \
		-B build \
		-S "$_pkgname" \
		-DCMAKE_BUILD_TYPE=None \
		-DCMAKE_INSTALL_PREFIX=/usr \
		-DCMAKE_SKIP_INSTALL_RPATH=ON \
		-DBUILD_TESTING=OFF \
		-DKDE_INSTALL_SYSCONFDIR=/etc
	make -C build
}

package() {
	make DESTDIR="${pkgdir}/" -C build install
}
